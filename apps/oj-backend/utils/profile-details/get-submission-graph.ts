import type { Platform } from "@repo/types/contest";

// TODO: Add logic to get the activity heatmap
export const getSubmissionGraph =async(username: string, platform: Platform, duration: string)=>{

}